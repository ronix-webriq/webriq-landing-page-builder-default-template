import React from "react"
import variant_a from "./variant_a.png"
import variant_b from "./variant_b.png"
import variant_c from "./variant_c.png"
import variant_d from "./variant_d.png"
import variant_e from "./variant_e.png"
import variant_f from "./variant_f.png"
import variant_g from "./variant_g.png"
import variant_h from "./variant_h.png"
import styles from "../../styles/variants.css"

export const variantA = () => {
  return <img src={variant_a} className={styles.feature} />
}

export const variantB = () => {
  return <img src={variant_b} className={styles.feature} />
}

export const variantC = () => {
  return <img src={variant_c} className={styles.feature} />
}

export const variantD = () => {
  return <img src={variant_d} className={styles.feature} />
}

export const variantE = () => {
  return <img src={variant_e} className={styles.feature} />
}

export const variantF = () => {
  return <img src={variant_f} className={styles.feature} />
}

export const variantG = () => {
  return <img src={variant_g} className={styles.feature} />
}

export const variantH = () => {
  return <img src={variant_h} className={styles.feature} />
}
